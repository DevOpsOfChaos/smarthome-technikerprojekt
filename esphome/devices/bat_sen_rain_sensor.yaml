# bat_sen_020 - Regensensor als MQTT-only Ersatzlinie

substitutions:
  node_name: bat-sen-020
  device_id: bat_sen_020
  device_name: BAT-SEN Rain
  device_class: bat_sen
  power_type: battery
  fw_version: "1"
  caps: "768"
  meta_schema_version: "1"
  control_mode: none
  config_profile: none
  reporting_mode: sleep_event
  sensor_mask: RXXXXXXXXX
  input_mask: XXXXX
  rain_pin: GPIO3
  battery_pin: GPIO4
  rain_wet_threshold_raw: "2200"
  rain_clear_threshold_raw: "2050"
  wake_interval_s: "900"
  rx_window_ms: "5000"

packages:
  contract_base: !include ../packages/smarthome_contract_base.yaml
  command_ack: !include ../packages/smarthome_command_ack.yaml

esphome:
  name: ${node_name}
  friendly_name: ${device_name}
  on_boot:
    priority: -10
    then:
      - lambda: |-
          id(contract_online) = true;
      - script.execute: publish_contract_meta
      - script.execute: publish_contract_availability
      - script.execute: publish_bat_rain_state
      - safe_mode.mark_successful

esp32:
  board: esp32-c3-devkitm-1
  toolchain: platformio
  framework:
    type: arduino

logger:
  level: WARN

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  fast_connect: true
  power_save_mode: LIGHT
  ap:
    ssid: ${node_name}-setup
    password: ${setup_ap_password}

mqtt:
  broker: !secret mqtt_broker
  username: !secret mqtt_username
  password: !secret mqtt_password
  topic_prefix: smarthome/device/${device_id}/internal
  discovery: false
  log_topic: null
  shutdown_message: null
  birth_message:
    topic: smarthome/device/${device_id}/availability
    retain: true
    payload: '{"device_id":"${device_id}","availability":"online","online":true,"power_type":"${power_type}"}'
  will_message:
    topic: smarthome/device/${device_id}/availability
    retain: true
    payload: '{"device_id":"${device_id}","availability":"offline","online":false,"power_type":"${power_type}"}'
  on_json_message:
    - topic: smarthome/device/${device_id}/command
      then:
        - lambda: |-
            std::string request_id = "";
            if (x["request_id"].is<const char*>()) {
              request_id = x["request_id"].as<std::string>();
            }
            if (request_id.empty()) {
              id(contract_fault) = true;
              id(contract_last_status_code) = -20;
              id(contract_last_status_text) = "missing_request_id";
              return;
            }

            std::string command = "";
            if (x["command"].is<const char*>()) {
              command = x["command"].as<std::string>();
            }

            auto queue_ack = [&](const char *status, int status_code, const char *source) {
              id(ack_request_id) = request_id;
              id(ack_status) = status;
              id(ack_status_code) = status_code;
              id(ack_source) = source;
              id(flush_command_ack).execute();
            };

            if (command == "get_state") {
              id(clear_contract_fault).execute();
              id(publish_bat_rain_state).execute();
              queue_ack("ok", 0, "node_ack");
              return;
            }

            if (command == "set_config") {
              if (!x["values"].is<JsonObjectConst>()) {
                id(contract_fault) = true;
                id(contract_last_status_code) = -21;
                id(contract_last_status_text) = "invalid_payload";
                queue_ack("invalid_payload", -21, "device_validation");
                return;
              }

              JsonObjectConst values = x["values"].as<JsonObjectConst>();
              bool changed = false;
              bool invalid = false;
              if (values["master_mac"].is<const char*>()) {
                std::string master_mac = values["master_mac"].as<std::string>();
                auto is_hex = [](char c) {
                  return (c >= '0' && c <= '9') || (c >= 'A' && c <= 'F') || (c >= 'a' && c <= 'f');
                };
                bool valid = master_mac.length() == 17;
                for (size_t i = 0; valid && i < master_mac.length(); i++) {
                  if ((i + 1) % 3 == 0) valid = master_mac[i] == ':';
                  else valid = is_hex(master_mac[i]);
                }
                if (valid) {
                  id(stored_master_mac) = master_mac;
                  id(setup_master_mac_text).publish_state(master_mac);
                  changed = true;
                } else {
                  invalid = true;
                }
              }

              if (invalid || !changed) {
                id(contract_fault) = true;
                id(contract_last_status_code) = -21;
                id(contract_last_status_text) = "invalid_payload";
                queue_ack("invalid_payload", -21, "device_validation");
                return;
              }

              id(clear_contract_fault).execute();
              id(publish_contract_meta).execute();
              id(publish_contract_availability).execute();
              id(publish_bat_rain_state).execute();
              queue_ack("ok", 0, "node_ack");
              return;
            }

            id(contract_fault) = true;
            id(contract_last_status_code) = -2;
            id(contract_last_status_text) = "unsupported";
            queue_ack("unsupported", -2, "master_validation");

deep_sleep:
  id: deep_sleep_1
  run_duration: ${rx_window_ms}ms
  sleep_duration: ${wake_interval_s}s

globals:
  - id: rain_detected_state
    type: bool
    restore_value: no
    initial_value: "false"
  - id: rain_raw_value
    type: int
    restore_value: no
    initial_value: "0"
  - id: battery_has_value
    type: bool
    restore_value: no
    initial_value: "false"
  - id: battery_mv_value
    type: int
    restore_value: no
    initial_value: "0"
  - id: battery_pct_value
    type: int
    restore_value: no
    initial_value: "0"

script:
  - id: publish_bat_rain_state
    then:
      - mqtt.publish_json:
          topic: smarthome/device/${device_id}/state
          retain: true
          payload: |-
            root["device_id"] = "${device_id}";
            if (id(battery_has_value)) root["battery_pct"] = id(battery_pct_value); else root["battery_pct"] = nullptr;
            if (id(battery_has_value)) root["battery_mv"] = id(battery_mv_value); else root["battery_mv"] = nullptr;
            root["window_open"] = nullptr;
            root["rain_raw"] = id(rain_raw_value);
            root["button_flags"] = 0;
            root["fault"] = id(contract_fault);

sensor:
  - platform: adc
    id: rain_adc
    pin: ${rain_pin}
    update_interval: 1s
    attenuation: 12db
    on_value:
      then:
        - lambda: |-
            id(rain_raw_value) = (int) lroundf(x * 4095.0f);
            if (id(rain_raw_value) >= atoi("${rain_wet_threshold_raw}")) {
              id(rain_detected_state) = true;
            } else if (id(rain_raw_value) <= atoi("${rain_clear_threshold_raw}")) {
              id(rain_detected_state) = false;
            }
        - script.execute: publish_bat_rain_state
  - platform: adc
    id: battery_adc
    pin: ${battery_pin}
    update_interval: 1s
    attenuation: 12db
    on_value:
      then:
        - lambda: |-
            int mv = (int) lroundf(x * 1000.0f * 2.0f * 1.078f);
            int pct = (mv - 2000) * 100 / (3200 - 2000);
            if (pct < 0) pct = 0;
            if (pct > 100) pct = 100;
            id(battery_mv_value) = mv;
            id(battery_pct_value) = pct;
            id(battery_has_value) = true;
        - script.execute: publish_bat_rain_state
